import { ButtonStylerDirective } from './button-styler.directive';

describe('ButtonStylerDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonStylerDirective();
    expect(directive).toBeTruthy();
  });
});
