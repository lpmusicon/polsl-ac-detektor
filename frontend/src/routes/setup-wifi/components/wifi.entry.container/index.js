import styled from "styled-components";

const WifiEntryContainer = styled.div`
  display: grid;
  grid-auto-flow: row;
  height: 100%;
  overflow: auto;
  gap: 12px;
`;

export default WifiEntryContainer;
